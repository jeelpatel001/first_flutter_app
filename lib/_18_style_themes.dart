import 'package:flutter/material.dart';

class MyStyleTheme extends StatelessWidget {
  const MyStyleTheme({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Theme Style"),
      ),
    );
  }
}
